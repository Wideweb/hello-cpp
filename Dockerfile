
# Запуск ---------------------------------------

# В качестве базового образа используем ubuntu:latest
FROM ubuntu:latest

# Скопируем директорию в контейнер
ADD ./build /app

# Установим рабочую директорию нашего приложения
WORKDIR /app/hello-bin

# Установим точку входа
ENTRYPOINT ["./hello"]